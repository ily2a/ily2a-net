// JS breakpoints — only MOBILE is used in JS (for navbar/hero layout switching).
// Other breakpoints are handled purely in CSS via Tailwind's tab:/md:/lg:/xl:/desk: variants.
export const BREAKPOINTS = {
  MOBILE: 600, // compact navbar / stacked hero layout
}
